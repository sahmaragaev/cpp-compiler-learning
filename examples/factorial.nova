function int factorial(int n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

function void main() {
    int num = 5;
    int result = factorial(num);
 
    print(num);
    print("factorial is:");
    print(result);
}